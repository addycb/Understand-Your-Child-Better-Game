/// @description Insert description here

event_inherited()

_event_instance = instance_create_layer(x, y, "Instances", o_event)
_text = "testingtext"
_schedule_index = 0

function draw_button(text){
	draw_text(x, y, text)
}

